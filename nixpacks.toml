[phases.setup]
nixPkgs = ["python311", "nodejs", "chromium"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install --with-deps chromium"
]

[start]
cmd = "python -m playwright install chromium && python bot.py"
